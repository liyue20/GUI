# Markdown 输入规范文档

## 基本概述

本文档提供了在AIGUI中使用 Markdown 格式的详细规范。（Markdown输入严格遵循目前主流的规范就可以解析，项目另外提供了图表解析的功能。）

## 基础语法规范

### 标题

使用 `#` 符号创建标题，层级从 1-6 不等：

```markdown
# 一级标题
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题
```

**注意**：文档中最高级别的标题会被视为生成block块的主标题，其他级别的标题会被视为子区块。

### 段落与文本格式

段落之间使用空行分隔：

```markdown
这是第一个段落。

这是第二个段落。
```

文本格式化：

- **加粗**：`**加粗文本**` 或 `__加粗文本__`
- *斜体*：`*斜体文本*` 或 `_斜体文本_`
- 链接：`[链接文本](http://example.com)`
- 图片：`![替代文本](图片URL "可选标题")`

### 列表

无序列表使用 `-`、`*` 或 `+` 符号：

```markdown
- 第一项
- 第二项
- 第三项
```

有序列表使用数字加点：

```markdown
1. 第一项
    1. 无序子项1
    2. 无序子项2
    
2. 第二项
    1. 有序子项1
    2. 有序子项2
```

1. 第一项
    1. 无序子项1
    1. 无序子项2
        1. 有序子项1
        2. 有序子项2
2. 第二项
    1. 有序子项1
        - 无序孙项1
        - 无序孙项2
    2. 有序子项2

### 引用块

使用 `>` 符号创建引用块：

```markdown
> 这是一个引用。
> 
> 它可以跨多行。
```

### 代码

行内代码使用单个反引号：`` `代码` ``

代码块使用三个反引号，可以指定语言：

```markdown
```python
def hello_world():
    print("Hello, world!")
```

```
```python
def hello_world():
    print("Hello, world!")
```

### 表格

标准 Markdown 表格格式

```markdown
| 表头1 | 表头2 | 表头3 |
| ----- | ----- | ----- |
| 单元格1 | 单元格2 | 单元格3 |
| 单元格4 | 单元格5 | 单元格6 |
```

| 表头1 | 表头2 | 表头3 |
| ----- | ----- | ----- |
| 单元格1 | 单元格2 | 单元格3 |
| 单元格4 | 单元格5 | 单元格6 |

## 图表特性

我们的系统支持通过特殊标记和表格数据生成多种类型的图表。

### 图表语法

图表的基本语法是：在表格前添加 `[chart:图表类型]` 标记，然后紧接着一个标准的 Markdown 表格。

```markdown
[chart:bar]
| 类别 | 数值 |
| ---- | ---- |
| A | 10 |
| B | 20 |
| C | 15 |
```

### 支持的图表类型

我们系统支持以下类型的图表：

1. **柱状图**：`[chart:bar]`
   
   - 适用于比较不同类别的数量
   - 第一列为类别，第二列为数值
2. **横向柱状图**：`[chart:barh]`
   
   - 横向显示的柱状图
   - 第一列为类别，第二列为数值
3. **饼图**：`[chart:pie]`
   
   - 适用于显示部分与整体的关系
   - 第一列为类别，第二列为数值
4. **折线图**：`[chart:line]`
   
   - 适用于显示数据随时间的变化趋势
   - 第一列通常为时间，第二列为数值
5. **散点图**：`[chart:scatter]`
   
   - 适用于显示两个变量之间的关系
   - 第一列和第二列为两个不同的变量
6. **面积图**：`[chart:area]`
   
   - 类似于折线图，但折线下方区域会被填充
   - 第一列通常为时间，第二列为数值
7. **雷达图**：`[chart:radar]`
   
   - 适用于多维数据的比较
   - 列名为维度，数值为各维度的得分
8. **堆叠柱状图**：`[chart:stacked_bar]`
   
   - 适用于显示部分与整体随时间的变化
   - 第一列为类别，其他列为不同组的数值
9. **分组柱状图**：`[chart:grouped_bar]`
   
   - 适用于比较不同组的多个类别
   - 第一列为类别，其他列为不同组的数值
10. **分组折线图**：`[chart:grouped_line]`
    
    - 适用于比较多个数据系列的趋势
    - 第一列通常为时间，其他列为不同系列的数值
11. **热力图**：`[chart:heatmap]`
    
    - 适用于显示矩阵数据的强度
    - 需要提供完整的数据矩阵
12. **箱型图**：`[chart:boxplot]`
    
    - 适用于显示数据分布情况
    - 每列代表一个要分析的数据集
13. **密度图**：`[chart:density]`
    
    - 适用于显示数据分布的概率密度
    - 第二列为要分析的数值数据

### 图表示例

#### 柱状图示例

```markdown
[chart:bar]
| 月份 | 销售额 |
| ---- | ------ |
| 一月 | 1200 |
| 二月 | 1500 |
| 三月 | 1800 |
| 四月 | 1400 |
```

#### 饼图示例

```markdown
[chart:pie]
| 产品 | 市场份额 |
| ---- | -------- |
| 产品A | 35 |
| 产品B | 25 |
| 产品C | 20 |
| 产品D | 20 |
```

#### 分组折线图示例

```markdown
[chart:grouped_line]
| 日期 | 产品A | 产品B | 产品C |
| ---- | ----- | ----- | ----- |
| 2023-01 | 100 | 150 | 120 |
| 2023-02 | 120 | 140 | 130 |
| 2023-03 | 140 | 130 | 150 |
| 2023-04 | 130 | 160 | 140 |
```

## 注意事项

1. **表格格式**：确保表格格式正确，包括表头、分隔行和数据行。每一行都必须以 `|` 开始和结束。
2. **数据类型**：图表中的数值必须是有效的数字，系统会尝试将文本转换为数值。
3. **中文支持**：系统支持中文字符，可以放心使用中文作为标签、标题等。
4. **图表大小**：生成的图表大小会根据数据自动调整，但过多的数据可能会影响可读性。
5. **数据一致性**：确保每行数据的列数相同，否则可能导致解析错误。

```

```
